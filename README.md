# SMPL 线性混合蒙皮（LBS）实验报告

## 一、实验目的

本实验基于 SMPL（Skinned Multi-Person Linear Model）模型完成一次完整的 Linear Blend Skinning（LBS）蒙皮过程可视化，具体目标包括：

1. 理解参数化人体模型中模板网格、形状参数、姿态参数、关节回归器和蒙皮权重之间的关系
2. 深入理解 LBS 的四个阶段：模板网格与权重、形状校正、姿态校正、完整蒙皮
3. 学会调用 SMPL 模型，并将官方 `lbs()` 实现中的关键中间量单独提取出来进行可视化
4. 验证手写 LBS 实现与官方前向结果的一致性

---

## 二、实验原理

### 2.1 LBS 四个阶段

#### (a) 模板网格与蒙皮权重

初始状态是模板人体网格 $$\bar{T}$$，通常处于 T-pose。每个顶点都带有一组对各关节的影响权重 $$\mathcal{W}$$。这一步的重点是理解：网格尚未根据人物体型改变，也未根据姿态弯曲，但每个顶点已经知道"将来应该主要跟着哪些骨骼走"。

#### (b) 形状校正 $$B_S(\beta)$$

形状参数 $$\beta$$ 控制人体的体型特征（高矮、胖瘦、肩宽、腿长等）。形状校正后得到：
$$T_{shape} = \bar{T} + B_S(\beta)$$

然后利用关节回归器从形状后的网格中回归出关节位置：
$$J(\beta) = \mathcal{J}(T_{shape})$$

关节位置不是固定常数，而是由形状后的网格回归出来的。

#### (c) 姿态校正 $$B_P(\theta)$$

人体在弯曲时，肩膀、肘部、膝盖附近会出现额外的几何变化，仅靠骨骼刚体旋转无法表达。因此 SMPL 在进入真正的 LBS 前，会加入姿态校正项：
$$T_P(\beta,\theta) = \bar{T} + B_S(\beta) + B_P(\theta)$$

实现思路是将姿态参数转换为旋转矩阵，构造 `pose_feature = R - I`，然后通过 `posedirs` 线性映射得到 `pose_offsets`。

#### (d) 线性混合蒙皮

经过上述步骤后，进入真正的 LBS：
$$v_i' = \sum_{k=1}^{K} w_{ik} \, G_k(\theta, J(\beta)) \begin{bmatrix} v_i^{posed} \\ 1 \end{bmatrix}$$

每个顶点最终不是只跟着一个关节走，而是跟着多个关节做加权平均后的变换，这也是"Linear Blend Skinning"名字的来源。

### 2.2 五个核心对象

| 对象 | 含义 |
|------|------|
| `v_template` | 模板顶点（T-pose） |
| `v_shaped` | 加了形状形变后的顶点 |
| `J` | 由 `v_shaped` 回归出的关节 |
| `v_posed` | 加了姿态校正后的顶点 |
| `verts` | 完成 LBS 之后的最终顶点 |

---

## 三、实验环境配置

### 3.1 环境要求

- Python 3.10+
- PyTorch（CPU 版即可）
- NumPy、Matplotlib
- SMPLX（SMPL 模型加载库）

### 3.2 安装步骤

```bash
# 创建 Conda 环境
conda create -n cg-lbs python=3.10 -y
conda activate cg-lbs

# 安装依赖
pip install torch numpy matplotlib smplx
```

### 3.3 模型文件准备

将 SMPL 模型文件 `SMPL_NEUTRAL.pkl` 放置于以下目录：
```
models/smpl/SMPL_NEUTRAL.pkl
```

---

## 四、实验步骤与结果

### 任务 1：加载 SMPL 模型并输出基础信息

使用 `smplx.create()` 加载 SMPL 模型，指定 `model_type='smpl'` 和 `gender='neutral'`。

**输出结果：**

| 指标 | 值 |
|------|-----|
| 顶点数 | 6890 |
| 面片数 | 13776 |
| 关节数 | 24 |
| Betas 维度 | 10 |

### 任务 2：可视化模板网格与蒙皮权重

#### (1) 单关节权重热力图

显示模板网格，并将指定关节（实验中选择关节 ID 18，即左肩）对所有顶点的影响权重可视化成颜色。

**输出文件：** `outputs/stage_a_template_weights.png`

**结果分析：**
- 颜色越明显（接近黄色/白色）表示该关节对该区域的影响越强
- 可以看到左肩附近区域颜色最深，表明这些顶点主要受左肩关节控制
- 远离左肩的区域（如腿部）颜色较浅，表明受影响较小

#### (2) 全关节主导权重分布图

**输出文件：** `outputs/all_joint_weights.png`

**结果分析：**
- 每个面片根据"主导影响关节"分配不同颜色
- 颜色明暗表示该主导权重的强弱
- 从图中可以看出 SMPL 模板网格在初始状态下已经携带了完整的关节影响分布信息

### 任务 3：可视化形状校正与关节回归

设置非零的形状参数 $$\beta$$，前三个参数设置为：$$\beta_0=2.0$$, $$\beta_1=-1.2$$, $$\beta_2=0.8$$。

**输出文件：** `outputs/stage_b_shaped_joints.png`

**结果分析：**
- 形状变化后的网格与模板网格相比，体型发生了明显变化
- 回归出的关节点叠加在身体内部合理位置
- 关节位置随体型变化而调整，例如人物变胖时，肩、膝、髋等关节位置也会相应变化

### 任务 4：可视化姿态校正 $$B_P(\theta)$$

设置非零姿态参数，包括：
- 左/右肩旋转
- 左/右肘弯曲
- 左/右髋旋转
- 左/右膝弯曲

**输出文件：** `outputs/stage_c_pose_offsets.png`

**结果分析：**
- 将 `pose_offsets` 的大小可视化成颜色，颜色越深表示偏移量越大
- 姿态校正主要集中在发生弯曲的部位附近（如肘部、膝盖、肩膀）
- 这一阶段尚未进行真正的蒙皮，只是说明网格本身已因姿态发生了额外修正

### 任务 5：可视化完整 LBS 结果

根据运动学树计算每个关节的全局刚体变换，用 `lbs_weights` 对这些关节变换加权，得到最终顶点。

**输出文件：** `outputs/stage_d_lbs_result.png`

**结果分析：**
- 人体已进入最终姿态，网格随骨骼运动而变形
- 关节位置也发生了变换（`J_transformed`）
- LBS 保证了皮肤在关节弯曲时的平滑过渡

### 任务 6：生成总对比图

将四个阶段排成一张 2×2 的对比图。

**输出文件：** `outputs/comparison_grid.png`

**对比分析：**
- (a) 模板网格 + 权重：网格处于 T-pose，颜色表示关节影响权重
- (b) 形状混合 + 关节回归：体型发生变化，关节位置相应调整
- (c) 姿态校正：网格因姿态发生局部修正
- (d) 最终蒙皮结果：网格随骨骼完成最终变形

### 任务 7：手写 LBS 与官方前向结果一致性验证

使用与手写实现完全相同的 `betas`、`global_orient` 和 `body_pose`，调用官方模型前向，得到 `output.vertices`，然后逐顶点比较。

**误差指标：**

| 指标 | 值 |
|------|-----|
| 平均绝对误差（MAE） | 0.0000000000 |
| 最大绝对误差（Max AE） | 0.0000000000 |

**结果分析：**
- 手写 LBS 实现与官方前向结果完全一致，误差为 0
- 证明手写实现准确复现了 SMPL 的 LBS 算法

---

## 五、选做内容：姿态动画

### 5.1 动画设置

- 固定形状参数（`betas` 全为 0）
- 让右肘关节从 0 弧度逐渐旋转到 1.5 弧度
- 生成 30 帧图片，导出 GIF 和 MP4

### 5.2 输出结果

**输出文件：**
- `outputs2/animation.gif`
- `outputs2/animation.mp4`
- `outputs2/frames/frame_000.png` ~ `frame_029.png`

### 5.3 观察效果

在动画中可以观察到：
1. 右肘关节逐渐弯曲
2. 权重区域（红色部分）随骨骼运动被平滑带动
3. 靠近肘部的区域始终显示较强的权重颜色
4. LBS 保证了皮肤在关节弯曲时平滑过渡，没有撕裂或突变

---

## 六、思考问题回答

### 6.1 关于蒙皮权重

**Q：为什么一个顶点不只受一个关节影响？**

**A：** 如果一个顶点只受一个关节影响，当骨骼运动时，该顶点会完全跟随该关节运动，导致关节附近出现"撕裂"现象。多个关节共同影响可以实现平滑的皮肤过渡。

**Q：如果一个顶点的权重几乎全给了某一个关节，会出现什么效果？**

**A：** 该顶点会几乎完全跟随该关节运动，在关节弯曲时可能出现皮肤不自然的拉伸或挤压。

**Q：如果权重分布很平均，又会出现什么效果？**

**A：** 顶点会被多个关节共同影响，运动更平滑，但可能导致关节处缺乏刚性，出现"果冻"效果。

### 6.2 关于关节回归

**Q：为什么关节位置要从形状后的网格回归，而不是固定不变？**

**A：** 因为关节位置依赖于人体体型。当人物变胖/变瘦时，肩、膝、髋等关节的位置也会相应变化。如果关节位置固定不变，会导致骨骼与皮肤不匹配。

### 6.3 关于姿态校正

**Q：为什么 LBS 之前还要加 pose corrective？**

**A：** 人体在弯曲时，关节附近会出现肌肉鼓起等几何变化，仅靠骨骼刚体旋转无法准确表达。Pose corrective 可以模拟这些细节变化，使蒙皮结果更加真实。

**Q：如果去掉 pose_offsets，最终人体弯曲处会出现什么问题？**

**A：** 关节弯曲处会出现不自然的凹陷或尖锐，无法模拟真实人体的肌肉形态。

### 6.4 关于线性混合蒙皮

**Q：J 和 J_transformed 有什么区别？**

**A：** `J` 是回归出的关节位置（在形状空间中），而 `J_transformed` 是经过运动学链变换后的关节位置（在世界空间中）。

**Q：为什么最终顶点要写成加权和，而不是只选择最大权重的关节？**

**A：** 使用加权和可以实现平滑的皮肤过渡，避免关节处的撕裂。如果只选择最大权重的关节，会导致皮肤在关节边界处不连续。

---

## 七、结论

本实验成功完成了 SMPL 模型的 LBS 蒙皮过程可视化，主要成果包括：

1. ✅ 成功加载 SMPL 模型，输出了基础模型信息
2. ✅ 完成了 LBS 四个阶段的可视化：模板网格与权重、形状校正、姿态校正、完整蒙皮
3. ✅ 实现了手写 LBS 算法，并验证了与官方前向结果的一致性（误差为 0）
4. ✅ 生成了姿态动画，观察了权重区域随骨骼运动的平滑带动效果
5. ✅ 回答了实验教程中的思考问题，加深了对 LBS 原理的理解

实验结果表明，Linear Blend Skinning 通过形状混合、姿态校正和线性权重混合，能够实现平滑、自然的人体蒙皮效果。

---

## 八、项目结构

```
cg_lab8/
├── run_lbs_lab.py          # LBS 实验主程序
├── run_animation.py        # 姿态动画脚本
├── src/
│   └── lbs.py              # LBS 相关工具函数
├── models/
│   └── smpl/
│       └── SMPL_NEUTRAL.pkl  # SMPL 模型文件
├── outputs/                # LBS 实验输出
│   ├── stage_a_template_weights.png
│   ├── stage_b_shaped_joints.png
│   ├── stage_c_pose_offsets.png
│   ├── stage_d_lbs_result.png
│   ├── comparison_grid.png
│   ├── all_joint_weights.png
│   └── summary.txt
├── outputs2/               # 动画输出
│   ├── animation.gif
│   ├── animation.mp4
│   └── frames/
├── pyproject.toml          # 项目依赖配置
└── .gitignore              # Git 忽略配置
```

---

## 九、运行命令

### 运行 LBS 实验

```bash
python run_lbs_lab.py --model-dir ./models --out-dir ./outputs --joint-id 18
```

### 运行姿态动画

```bash
python run_animation.py --model-dir ./models --out-dir ./outputs2 --num-frames 30 --fps 30
```

---

## 十、参考文献

1. Loper, M., Mahmood, N., Romero, J., Pons-Moll, G., & Black, M. J. (2015). SMPL: A Skinned Multi-Person Linear Model. ACM Trans. Graphics.
2. SMPL Official Website: https://smpl.is.tue.mpg.de